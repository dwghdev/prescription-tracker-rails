<%= form_with model: [@doctor, prescription] do |f| %>
  <% if prescription.errors.any? %>
    <div class="error_explanation">
      <ul>
        <% prescription.errors.each do |error| %>
          <li> <%= error.full_message %> </li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% end %>
